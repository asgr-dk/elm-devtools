{
  "tasks": {
    "try:build": "deno run -A source/Elm.js && deno run -A source/Main.js 0.2.0 build",
    "compile": "deno run -A source/Elm.js optimize && deno compile --unstable-bundle --output build/elm-devtools source/Main.ts 0.2.0"
  }
}
